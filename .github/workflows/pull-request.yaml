name: Pull Requests

on:
  workflow_dispatch:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Set up Python 3.13
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.13"

      - name: Install uv
        uses: astral-sh/setup-uv@38f3f104447c67c051c4a08e39b64a148898af3a # v4

      - name: Install dependencies
        run: uv sync

      - name: Check formatting
        if: github.event_name == 'pull_request'
        run: uv run ruff format --check .

      - name: Run linting
        run: uv run ruff check src/ tests/

      - name: Run type checking
        run: uv run basedpyright src/

      - name: Run unit tests
        run: uv run pytest tests/unit -v

      - name: Verify package imports
        run: |
          uv run python -c "from aiodynamo.client import Client; print('Client imported successfully')"
          uv run python -c "from aiodynamo.models import KeySchema, KeySpec, KeyType; print('Models imported successfully')"
          uv run python -c "from aiodynamo.expressions import MultiHashKey, MultiHashAndRangeKeyCondition; print('Multi-key expressions imported successfully')"
